class Localizacao {
  int idlocalizacao;
  String localizacao;
  bool incluir;

  Localizacao(int idlocalizacao, String localizacao, bool incluir) {
    this.idlocalizacao = idlocalizacao;
    this.localizacao = localizacao;
    this.incluir = incluir;
  }
}
